# Test-focused development environment
# This configuration optimizes for fast, offline testing

# Cache Configuration - Use in-memory for speed
FINOS_MCP_ENABLE_CACHE=true
FINOS_MCP_CACHE_MAX_SIZE=100
FINOS_MCP_CACHE_TTL_SECONDS=60

# Logging - Reduce noise during testing
FINOS_MCP_LOG_LEVEL=WARNING
FINOS_MCP_DEBUG_MODE=false

# Network - Faster timeouts for testing
FINOS_MCP_HTTP_TIMEOUT=5

# GitHub - Do not configure a token in the test environment; tests that need
# one should set FINOS_MCP_GITHUB_TOKEN to a valid-format placeholder via
# fixture/mock rather than loading this file directly with validate_github_token
# active.  The validator rejects values that lack a recognised GitHub prefix
# (ghp_, gho_, ghu_, ghs_, ghr_, ghv_, github_pat_), so a bare mock string
# would fail startup validation.
# FINOS_MCP_GITHUB_TOKEN=
